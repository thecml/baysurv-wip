# Network
network_layers: [64]
activiation_fn: "relu"
dropout_rate: 0.25
l2_reg: 0.01

optimizer:
  class_name: SGD
  config:
    learning_rate: 0.01
    momentum: 0
    decay: 0

loss_fn:
  class_name: CoxPHLoss
  config:
    reduction: 'auto'
    name: None